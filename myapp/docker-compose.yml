version: "3.9"

services:
  web:
    build: .
    container_name: my-fastapi-app
    ports:
      - "8000:8000"
    environment:
      - MONGO_URI=mongodb://host.docker.internal:27017/
    volumes:
      - .:/app
